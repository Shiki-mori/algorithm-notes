# 冒泡排序

第一轮

比较a[0]a[1]，大的放后面；
比较a[1]a[2]，大的放后面；
……
比较a[n-2]a[n-1]，大的放后面；

第二轮

比较到a[n-3]a[n-2]

## 异或运算交换数组元素

异或可理解为无进位相加。

异或的性质：

```text
0^n=n
n^n=0
满足交换律和结合律。
```

使用异或交换元素，必须保证i和j指向不同的内存。但其值可以相等。

## 复杂度

时间复杂度：O(n^2)  
空间复杂度：O(1)
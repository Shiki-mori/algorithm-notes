（1）整数数组中，一个数出现奇数次，其他所有数出现偶数次，找出出现奇数次的数。

（2）两个数出现奇数次，其他所有数出现偶数次。找出出现奇数次的数。

出现偶数次的数，异或结果为0。  
出现奇数次的数n，异或结果为n。

(1) 对数组异或即可。  
(2) 所有元素异或结果为两个奇数次数的异或结果eor=a^b。  
a!=b,eor!=0  
eor的所有位中，至少有一位不为0，a与b在该位上的值不同。  
令eor'异或所有在该位为1的数，则结果为a||b。
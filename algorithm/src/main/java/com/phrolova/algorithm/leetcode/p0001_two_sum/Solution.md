# 题解：Two Sum

## 思路

用哈希表边扫边查：遍历到 `nums[i]` 时，看表里是否已有 `target - nums[i]`。

- 有：返回这两个下标
- 没有：把当前值与下标放入表中，继续

一次遍历即可，避免双重循环。

## 复杂度

- 时间：O(n)
- 空间：O(n)

## 关键点

- 先查再放，避免同一个元素用两次（例如 `target = 6` 且只有一个 `3`）
- 题目保证恰好一组解，无需处理无解情况
